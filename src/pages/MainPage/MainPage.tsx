import {Htag, Layout} from "../../components";

export const MainPage = () => {

    return (
        <Layout>
            <Htag className={"page-title"} tag={'h1'}>Главная</Htag>
        </Layout>
    )
}

